<form method="post" action="{{ path('app_task_delete', {'id': task.id}) }}" onsubmit="return confirm(' {{ 'confirm.delete.task'|trans }} ')">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task.id) }}">
	<button class="btn btn-outline-danger btn-sm d-inline-flex align-items-center gap-2">        <i class="bi bi-trash3"></i>
{{'form.delete'|trans}}</button>
</form>
