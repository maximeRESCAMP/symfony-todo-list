{% extends 'base.html.twig' %}

{% block title %}
{{'task.update' |trans}}
{% endblock %}

{% block body %}
 <div class="d-flex align-items-center justify-content-between mb-4">
        <h1 class="h4 fw-bold mb-0">{{ 'task.update'|trans }}</h1>
        <a href="{{ path('app_task_index') }}" class="btn btn-outline-secondary btn-sm d-inline-flex align-items-center gap-2">
            <i class="bi bi-arrow-left"></i>{{ 'task.back_page'|trans }}
        </a>
    </div>

{{ include('task/_form.html.twig', {'button_label': 'form.update'}) }}

<div class="d-flex justify-content-end mt-3 bg-danger">
	{{ include('task/_delete_form.html.twig') }}
</div>

{% endblock %}